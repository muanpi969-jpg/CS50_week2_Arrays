# Caesar

## What it does

Problem Set 2's Caesar exercise encrypts plaintext by rotating alphabetic characters by a numeric key.

No Caesar source file was present in the workspace; this directory is retained as a syllabus-aligned placeholder.

## Compile and use

Once a source file is added, compile it in a CS50 environment with `clang caesar.c -o caesar -lcs50`, then run `./caesar key`.
